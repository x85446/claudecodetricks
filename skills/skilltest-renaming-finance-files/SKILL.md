---
name: skilltest-renaming-finance-files
description: this tests the renaming skill for finance tasks and files"
---

lets practice renaming finance files. Always do the following:

1. Delete whatever is in $(pwd)/tmp/done and in $(pwd)/tmp/processfiles
2. copy the contents of $(pwd)/test/done to $(pwd)/tmp/processfiles
3. copy the contents of $(pwd)/test/done to $(pwd)/tmp/processfiles
4. process $(pwd)/tmp/processfiles moving the successful files to $(pwd)/tmp/done
