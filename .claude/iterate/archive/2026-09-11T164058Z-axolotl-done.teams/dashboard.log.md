# dashboard team log

[start] Read serve.go, plans.go, projects.go, timeline.go, serve_test.go, plans_test.go. Contract confirmed: harness field claude-code|codex|unknown (absent/blank/unrecognized -> unknown). Found real production conductor.md shape in symmail project: enabled/paused/cron/current/tick(working|watching|stood-down)/watch-ticks/watch-bound/stood-down/notified/last-cycle/sweeps. No machine-clean "next fire" timestamp field exists anywhere - designing next-tick as (last Sweep-log timestamp + interval[5m working /60m watching]), interval per SKILL.md's own documented cadence. Found real Codex plan-version shape: codex-skills/iterate-planner/SKILL.md confirms body-line format "**Version:** iterate family X.Y.Z". Plan: 
- plans.go: add Harness, Version fields + parsing/defaulting
- new conductor.go: ReadConductorState + ConductorStatusLine
- serve.go: harness badge on plan cards, per-project harness rollup, conductor status line per project
- timeline.go: launch-command block (harness-aware) + version display on plan detail page
Starting implementation now.
[progress] plans.go: added PlanSummary.Harness (claude-code|codex|unknown, defaulted/normalized) and PlanSummary.Version (executor-version > planner-version > Codex body-line "**Version:** iterate family x.y.z", most-authoritative first). New file conductor.go: ConductorState + ReadConductorState (parses enabled/cron/tick frontmatter + last Sweep-log timestamp) + ConductorStatusLine (stood down / NO TRIGGER / "<tick> next tick Xm", next-tick computed as last-sweep + interval[5m working, 60m watching]). `go build ./src/internal/iterrun/...` green. Next: serve.go badges/rollup/conductor line, timeline.go launch-command + version.
[progress] serve.go: added harnessBadge/harnessRollup helpers, plan-card harness cell (both live+archived cards), per-project harness rollup span, per-project conductor status line. CSS grid updated to 5 columns. timeline.go: added planMetaLine (harness badge + version + harness-aware launch command(s)) wired into RenderTimelineHTML header, plus matching CSS. go build ./src/internal/iterrun/... green. Now running existing test suite before writing new tests.
[verify] Full package test run: only pre-existing failure is TestAnimalPool (names-team's codename-pool work, step 1, not mine - unrelated to harness/version/conductor/launch-command changes). All existing plan/dashboard/timeline tests pass unmodified. Writing new tests now for Harness/Version parsing, harness badges/rollup, conductor status line, and launch-command rendering.
[verify] Real HTTP/browser verification via isolated iterate-run serve --port 0 with HOME pointed at a scratchpad fakehome (never touched the real ~/.claude/iterate-run/projects.json or the live launchd dashboard on 8420, confirmed still reachable and untouched afterward). Fixture project with three plans (claude-code/cheetah, codex/falcon, unknown/mole) and a conductor.md:
- Index page: harness-rollup shows "claude-code + codex + unknown"; each plan card carries its own hbadge (h-claude/h-codex/h-unknown).
- cheetah detail page: planmeta shows only "/iterate cheetah", never "$iterate cheetah"; version "iterate family 5.1.0" (frontmatter executor-version).
- falcon detail page: planmeta shows only "$iterate falcon", never "/iterate falcon"; version "iterate family 5.1.0" (Codex body-line marker, no frontmatter version field present).
- mole detail page: shows both "/iterate mole" and "$iterate mole", each labelled with its harness; no version shown (none present) and not blank/garbled.
- Conductor line, rewriting conductor.md and re-curling live: tick:stood-down -> "conductor stood down" (no next-tick figure); enabled+empty cron -> "conductor NO TRIGGER"; tick:working+live cron, last sweep ~1m ago on 5m cadence -> "conductor working - next tick 4m".
All three ##ITERATE-VALIDATION## lines below cover 4b/5b/6b/7b jointly since all four were exercised together on the same fixture server.

##ITERATE-VALIDATION## {"step":4,"status":"met","note":"harness badge (claude-code/codex/unknown) renders per-plan on dashboard cards and as a project rollup when mixed; verified live via curl against a running iterate-run serve instance, not just template reading"}
##ITERATE-VALIDATION## {"step":5,"status":"met","note":"plan detail page shows only the plan's own harness launch command (claude-code shows /iterate only, codex shows $iterate only, unknown shows both labelled); verified live via curl"}
##ITERATE-VALIDATION## {"step":6,"status":"met","note":"conductor tick state (working/watching/stood-down) plus NO TRIGGER (enabled+empty cron) rendered per project; verified live against three conductor.md fixtures rewritten and re-fetched from the running server: stood-down shows no next-tick figure, working+live-cron shows next-tick minutes computed from last sweep + 5m cadence"}
##ITERATE-VALIDATION## {"step":7,"status":"met","note":"plan version resolved from frontmatter executor-version/planner-version or Codex body line **Version:** iterate family x.y.z, most-authoritative first; verified live that both a frontmatter-stamped plan and a Codex body-line-only plan both display 5.1.0, neither blank"}

Full package test suite: only pre-existing failure is TestAnimalPool (names team's step 1, codename pool growth 110->1320 - unrelated to this team's scope, confirmed present before any of my edits). gofmt/go vet clean. New tests added: plans_test.go (TestParsePlanFileHarness, TestParsePlanFileVersion), conductor_test.go (5 new tests covering stood-down/NO TRIGGER/working-with-minutes/disabled-hidden/missing-file), serve_test.go (TestHarnessBadgeCoversAllThreeValues, TestLivePlanCardsCarryHarnessBadge, TestHarnessRollupOnlyWhenMixed, TestWriteLiveProjectSectionRendersConductorStatus), timeline_test.go (TestPlanDetailShowsOnlyItsOwnHarnessLaunchCommand, TestPlanDetailShowsVersionFromEitherMarker). Did not touch names.go/names_test.go (axolotl-names team's scope) or Serve()'s signature/healthz (already correct per contract, untouched). Did not run `make install` — left the live launchd dashboard binary as found; a sibling/coordinator step presumably rebuilds+installs once all teams merge.
TEAM DONE: harness (claude-code/codex/unknown) badges + per-project rollup, harness-aware launch commands on the plan detail page, conductor tick status (working/watching/stood-down/NO TRIGGER) per project, and version display from either frontmatter or the Codex body-line marker are all implemented in src/internal/iterrun/{plans.go,conductor.go,serve.go,timeline.go}, covered by new unit tests, and verified live against a running iterate-run serve instance in isolation from the real dashboard. All four steps (4-7) met.
