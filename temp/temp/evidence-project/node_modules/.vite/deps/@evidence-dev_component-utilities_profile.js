import "./chunk-VSE7GGL6.js";

// ../../node_modules/@evidence-dev/component-utilities/src/profile.js
import { dev } from "$app/environment";
function profile(f, ...args) {
  if (!dev || !import.meta.env.VITE_EVIDENCE_DEBUG) return f.call(this, ...args);
  const before = performance.now();
  const complete = () => {
    const after = performance.now();
    console.debug(`${f.name}() took ${(after - before).toFixed(0)}ms`);
  };
  const r = f.call(this, ...args);
  if (r instanceof Promise) {
    return r.then((promiseResult) => {
      complete();
      return promiseResult;
    });
  } else {
    complete();
    return r;
  }
}
export {
  profile
};
//# sourceMappingURL=@evidence-dev_component-utilities_profile.js.map
